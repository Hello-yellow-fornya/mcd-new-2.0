import { notFound } from 'next/navigation';

/** /report/ belongs to Claims Report Line; on Claims 24/7 the route is /claim-now/. */
export function ReportPage(): never {
  notFound();
}

export function ReportThanks(): never {
  notFound();
}
