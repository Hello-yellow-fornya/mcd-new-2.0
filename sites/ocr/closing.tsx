/** The section before the footer on every page: the navy "One call sorts the lot." (the claims-department band is banned on this site). */
export { FinalCta as Closing } from './components/FinalCta';
